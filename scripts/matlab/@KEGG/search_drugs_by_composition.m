function xReturn = search_drugs_by_composition(obj,composition)
%search_drugs_by_composition(obj,composition)
%
%     Input:
%       composition = (string)
%   
%     Output:
%       return{:} = (string)

% Build up the argument lists.
values = { ...
   composition, ...
   };
names = { ...
   'composition', ...
   };
types = { ...
   '{http://www.w3.org/2001/XMLSchema}string', ...
   };

% Create the message, make the call, and convert the response into a variable.
soapMessage = createSoapMessage( ...
    'SOAP/KEGG', ...
    'search_drugs_by_composition', ...
    values,names,types,'rpc');
response = callSoapService( ...
    obj.endpoint, ...
    'SOAP/KEGG#search_drugs_by_composition', ...
    soapMessage);
xReturn = parseSoapResponse(response);
