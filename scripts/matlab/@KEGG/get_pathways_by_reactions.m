function xReturn = get_pathways_by_reactions(obj,reaction_id_list)
%get_pathways_by_reactions(obj,reaction_id_list)
%
%     Input:
%       reaction_id_list{:} = (string)
%   
%     Output:
%       return{:} = (string)

% Build up the argument lists.
values = { ...
   reaction_id_list, ...
   };
names = { ...
   'reaction_id_list', ...
   };
types = { ...
   '{http://www.w3.org/2001/XMLSchema}string[]', ...
   };

% Create the message, make the call, and convert the response into a variable.
soapMessage = createSoapMessage( ...
    'SOAP/KEGG', ...
    'get_pathways_by_reactions', ...
    values,names,types,'rpc');
response = callSoapService( ...
    obj.endpoint, ...
    'SOAP/KEGG#get_pathways_by_reactions', ...
    soapMessage);
xReturn = parseSoapResponse(response);
